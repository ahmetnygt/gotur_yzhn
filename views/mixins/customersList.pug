each c in customers
    - const btnClass = c.isBlackList ? 'btn-outline-danger' : 'btn-outline-primary'
    .btn-group
        //- DÜZELTME: Sabit ".col-11" sınıfı, aşağıdaki dinamik class attribute'ünün
        //- hesapladığı "col-11"/"col-12" ile birleşiyordu (Pug bunları override etmez,
        //- birleştirir). Sonuç olarak yetkisiz kullanıcılarda buton "col-11 col-12"
        //- sınıflarının ikisini birden taşıyıp yanlış genişlikte görünüyordu.
        button.d-flex.btn.customer-row(
            type="button"
            class=`${btnClass} ${permissions.includes('TRIP_BLACKLIST_MANAGE')?"col-11":"col-12"}`
            data-id=c.id
            data-idnumber=c.idNumber
            data-name=c.name
            data-surname=c.surname
            data-phone=c.phoneNumber
            data-gender=c.gender
            data-customertype=c.customerType
            data-customercategory=c.customerCategory
            data-pointorpercent=c.pointOrPercent
            data-pointamount=c.point_amount
            data-percent=c.percent
        )
            .col-2
                p.text-center.m-0 #{c.idNumber}
            div(class=`${blacklist == "true"?"col-2":"col-3"}`)
                p.text-center.m-0 #{c.name}
            .col-2
                p.text-center.m-0 #{c.surname}
            .col-2
                p.text-center.m-0 #{c.phoneNumber}
            div(class=`${blacklist == "true"?"col-2":"col-3"}`)
                p.text-center.m-0 #{c.customerCategory == 'member' ? 'Abone' : ''}
            if blacklist == "true"
                .col-2
                    p.text-center.m-0 #{c.blackListDescription}
        if permissions.includes('TRIP_BLACKLIST_MANAGE')
            if c.isBlackList
                button.btn.btn-outline-primary.customer-blacklist-remove.col-1(type="button" data-id=c.id aria-label="Kara listeden çıkar")
                    i.fa-solid.fa-user
            else
                button.btn.btn-outline-danger.customer-blacklist-open.col-1(type="button" data-id=c.id aria-label="Kara listeye ekle")
                    i.fa-solid.fa-user-slash
